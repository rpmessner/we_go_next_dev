# Agent Context

Use [CLAUDE.md](./CLAUDE.md) as the durable project context file for this repository.

Before making architecture or task-board decisions, read `CLAUDE.md` first. It contains the current product direction, medallion/rules architecture, layer boundaries, task order, testing expectations, and session-documentation rules.

The Phoenix app lives in `we_go_next/`; repository-level documentation lives in `docs/`.
