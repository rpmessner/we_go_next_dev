# Gigalixir Elixir buildpack configuration.
# This file lives in we_go_next/ because CI deploys that subfolder as the app root.

erlang_version=28.5.0.2
elixir_version=1.19.5
release=true

hook_pre_compile="mix zig.get && mix assets.deploy"
